import React from 'react';

const AddMovie = (props) => (
  <div>
    <form>
      <input placeholder="Add movie title here"></input>
      <button>Add</button>
    </form>
  </div>
)

export default AddMovie;